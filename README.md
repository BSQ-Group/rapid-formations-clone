# QA evidence

Screenshots backing PR review, kept on an orphan branch so they never land in `main`.
Nothing here is built, imported or served. Safe to delete once the PRs referencing it are closed.

## refund-cancellation-policy

Captured at 1440x1000, DPR 2, against `https://www.rapidformations.co.uk/refund-cancellation-policy/`
and `https://localhost:3000/refund-cancellation-policy`. The live cookie banner is hidden before
capture so it does not cover the content.

- `nested-inset.png` — the first nested clause group, live / before / after. The red rule sits at the
  source's nested-item box edge (x=217).
- `full-page.png` — the whole page, live / before / after.

## whistleblowing-grievance-mechanism-policy

Same capture method.

- `nested-inset.png` — the "Reporting a Grievance or Complaint" clause, live / before / after.
  The red rule sits at the source's lettered sub-point box edge (x=215).
- `full-page.png` — the whole page, live / before / after.
