# QA evidence — package pages

Screenshots backing PR review, on an orphan branch so they never land in `main`.
Nothing here is built, imported or served. Safe to delete once the PR is closed.

Captured at 1440x1000, DPR 2, against `https://www.rapidformations.co.uk/package/basic-package/`
(the source serves these nested — the flat `/basic-package/` 404s) and
`https://localhost:3000/basic-package`. The live cookie banner is hidden before capture.

- `banking-section.png` — the banking partners section, live / before / after.
- `title-align.png` — the page title row, live / before / after.
