# task-state

Unprotected coordination branch for task-orchestration — task state, reports, board, and control logs. Do NOT put project code here; code lives on `main`. Tools resolve this worktree from git automatically (no env, no per-machine setup).
