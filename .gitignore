build
dist / media
node_modules
.DS_Store
.env
.env.qcf
.env.rf
.env.example
.env.local
.next
next-env.d.ts
.vercel
.claude/settings.json
.claude/tmp/

# SSL certificates for local HTTPS dev server
*.pem
.claude/worktrees/
tsconfig.tsbuildinfo
storybook-static
debug-storybook.log

# Payload default media upload directory
public/media/

public/robots.txt
public/sitemap*.xml


# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.playwright-mcp/

# Ad-hoc Playwright screenshots (visual QA captures)
/prod-*.png
/local-*.png
.gstack/

# Local Claude Code runtime artifacts
.claude/scheduled_tasks.lock

# Scratch from the gatsby-section-to-payload-block skill
page-*-backup-*.json
content.json
/shots/

# Per-section scratch from the porting skill (measurements, extracted content,
# downloaded assets). Regenerated on demand.
/port/
/page-compare/

# Port working notes -- the per-decision record and the section map. Local only.
.port-source-facts.json
.port-site-map.json

# Regenerable SEO parity harvest (produced by scripts/seo-audit/extract-seo.ts).
reports/seo-audit/seo-data.json
