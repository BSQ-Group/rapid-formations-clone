build
dist / media
node_modules
.DS_Store
.env
.env.qcf
.env.rf
.env.example
.env.local
.next
next-env.d.ts
.vercel
.claude/settings.json
.claude/tmp/

# SSL certificates for local HTTPS dev server
*.pem
.claude/worktrees/
tsconfig.tsbuildinfo
storybook-static
debug-storybook.log

# Payload default media upload directory
public/media/

public/robots.txt
public/sitemap*.xml


# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.playwright-mcp/

# Ad-hoc Playwright screenshots (visual QA captures)
/prod-*.png
/local-*.png
.gstack/

# Local Claude Code runtime artifacts
.claude/scheduled_tasks.lock

# Scratch from the gatsby-section-to-payload-block skill
page-*-backup-*.json
content.json
/shots/

# Per-section scratch from the porting skill (measurements, extracted content,
# downloaded assets). Regenerated on demand; the decisions live in
# .port-source-facts.json, which IS committed.
/port/
/page-compare/
