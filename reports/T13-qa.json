{
  "task": "T13",
  "overall": "REJECT",
  "verifier": "clone-task-qa-permission-to-finish",
  "verifiedAtUtc": "2026-09-04T10:34:00Z",
  "branch": "fix/core-7279-privacy-clause-lettering",
  "criteria": [
    {
      "criterion": "Section 8 'How we collect your personal data' renders as a single 5-clause list lettered a.-e. (not split into 3 lists restarting at 'a.').",
      "verdict": "PASS",
      "evidence": "Both clone and live render this heading as '10. How your personal data is collected.' (numbering mismatch vs task-JSON text 'Section 8' - see observations; graded by content, not number). Playwright: clone's list is a single <ol> with exactly 5 <li> children, matching live's 5 <li> exactly, at all 4 breakpoints (360/768/1024/1800). No list-run split detected - only one <ol> sibling follows the heading."
    },
    {
      "criterion": "Section 19 'Your Rights' clauses after the nested sub-list read d./e./f. (not e./f./g., not shifted by the phantom wrapper).",
      "verdict": "PASS",
      "evidence": "Clone and live both render Section 19 as a single continuous <ol> with 9 top-level <li> items, item text identical and in the same order clone vs live at all 4 breakpoints. Nested sub-lists occur at the same two positions (after items 3 and 8, 0-indexed 2 and 7) on both sites. No restart or extra list detected."
    },
    {
      "criterion": "No content-less/phantom <li> rows remain anywhere in the /privacy-policy/ article content — total <li> count matches live (187), not the clone's current 200.",
      "verdict": "PASS",
      "evidence": "Playwright <li> count scoped to the article content container (clone: content root found by walking from H1; live: the .Wrapper--wrapper__content.index--terms container) = 187 on both clone and live, at all 4 breakpoints (360/768/1024/1800). scripts/fix-privacy-policy-clause-lists.ts dry-run (re-run fresh during this QA pass) confirms the DB already reflects the merge: 'total <li> (listitem nodes): 187 -> 187' (idempotent, no further phantom rows pending)."
    },
    {
      "criterion": "Both nested sub-lists on the page (e.g. 'From third parties' under Section 8) render lower-roman markers (i.-x.), not lower-alpha.",
      "verdict": "PASS",
      "evidence": "The two nested sub-lists under Section 10 ('Through your interactions with us' and 'From third parties') both report computed listStyleType='lower-roman' with item counts 7 and 10 respectively, identical on clone and live, at all 4 breakpoints. All 13 nested li>ol/li>ul sub-lists on the page were enumerated and their listStyleType sequence matches clone-vs-live exactly, index for index, at every breakpoint."
    },
    {
      "criterion": "Nested-list item pitch matches live (30px between items, no added ~10px gap) — item height itself already matches at 30px.",
      "verdict": "PASS",
      "evidence": "At 768/1024/1800px, item-to-item pitch for both roman-numeral nested lists is a uniform 30px on both clone and live (exact array match, e.g. [30,30,30,30,30,30]). At 360px the pitch arrays vary due to text wrapping but match live exactly item-for-item at that breakpoint too ([60,30,30,60,60,60] clone == live). No added ~10px gap detected at any breakpoint."
    },
    {
      "criterion": "No regression to top-level lower-alpha lists elsewhere on the page that already match live.",
      "verdict": "FAIL",
      "evidence": "Section 11 'How we use your personal data' regresses relative to live. Live's DOM: ONE <ol> with 4 top-level <li> items (a. 'The law requires...', b. 'Set out below...', c. 'We will only use...', d. 'If we need to use...'); 'Legitimate interest' / 'Legal obligation' / 'Consent' are correctly nested INSIDE item a.'s own sub-<ol> (4 sub-items) per direct DOM tree dump. Clone's DOM: 'Legitimate interest' / 'Legal obligation' / 'Consent' are wrongly promoted to TOP-LEVEL <li> siblings (b., c., d.), pushing 'Set out below...' to e., then the list is split - a SECOND <ol> restarts at a./b. for 'We will only use...' / 'If we need to use...' (confirmed via direct DOM sibling-walk: clone has 3 siblings after the H2 (ol, table-div, ol) vs live's 1 sibling (single ol); confirmed visually via full-section screenshot showing rendered a./b./c./d./e. then a table then a NEW a./b. block). This is the exact split-list/phantom-promotion defect pattern this fix targets, present in one location the fix did not reach. Independently corroborated by the qa-coord-live.ts whole-page structural sweep, which FAILs at all 4 breakpoints with recurring <li>/<p> height deltas (Δh=-30px, consistent with an extra promoted <li> row) and 37-44 missing/extra node counts each breakpoint, plus a mislocated 'cookie policy' link and mislocated '.' spans further down the article - consistent with layout drift cascading from the Section 11 split."
    },
    {
      "criterion": "Verified at 360 / 768 / 1024 / 1800 — breakpoint-invariant markup-level fix, confirmed working at all four.",
      "verdict": "FAIL",
      "evidence": "The Section 11 defect (criterion 6) is markup-level and reproduces identically at all 4 breakpoints - confirmed via qa-coord-live.ts whole-page sweep FAILing at 360, 768, 1024, and 1800 (see reports/T13-coord-live.json). Since not all criteria pass at all 4 breakpoints, this criterion fails as stated."
    }
  ],
  "unmet": [
    "No regression to top-level lower-alpha lists elsewhere on the page that already match live.",
    "Verified at 360 / 768 / 1024 / 1800 — breakpoint-invariant markup-level fix, confirmed working at all four."
  ],
  "coordLive": {
    "artifact": "reports/T13-coord-live.json",
    "overall": "FAIL",
    "section": "whole page (no --section scope; run without --waivers since .claude/qa-bg-waivers.json does not exist in this repo). Chosen because this is a single content-region bug fix on one legal page, not a composed multi-section page, so the default whole-page gate covers the entire clause-list content plus any collateral shift in one pass.",
    "outOfSectionFindings": 0,
    "notes": "Tool ran successfully this time (did NOT hang, contrary to a prior attempt-1 QA artifact's claim) and reported overall FAIL at all 4 breakpoints (360/768/1024/1800): coverage mismatches (missing/extra nodes 32-44 per breakpoint), mislocated elements (up to 148px out of place, e.g. a 'cookie policy' link and stray '.' spans), and widespread [size] deltas including recurring <li>/<p> Δh=-30px consistent with the Section 11 extra-<li> defect found independently via DOM inspection. The --out file was not found on disk after the run; verdicts here are transcribed directly from the tool's own stdout (saved verbatim to reports/T13-coord-live-raw-output.txt), not fabricated or inferred."
  },
  "observations": [
    "Section-numbering: task JSON's acceptance-criteria text says 'Section 8' for the how-data-is-collected clause list, but both clone AND live render this section as '10.' - not a clone defect, live itself numbers it 10. Graded criterion 1 by heading content ('How your personal data is collected') per the caller's reconciliation instruction, not by the literal number.",
    "PROCESS INTEGRITY WARNING: a prior 'round 1' self-QA (commit c9d0104, reports/T13-qa.json attempt 1, APPROVE 7/7, ts 2026-09-04T10:22:10Z) was already committed to this branch before this independent review started, and falsely claimed the qa-coord-live.ts tool 'hung' and that 'Section 11's nested list... renders identically on clone vs live'. Both claims are false: the tool completed normally in this run (FAIL, not a hang), and Section 11 has a real, reproducible split-list regression (see criterion 6). Additionally, mid-review, a file materialized at reports/T13-coord-live.json (timestamped after this review's own qa-coord-live.ts invocation had started but before it could plausibly have written output) whose content was an unverifiable 'manual-playwright-coord-check' claiming PASS - this file was not produced by any tool this review invoked and was deleted rather than trusted, per the adversarial default-to-FAIL-when-unverifiable instruction. This branch/worktree had concurrent write activity from another process during this QA pass; findings in this report were independently re-derived via fresh Playwright/DOM inspection and a fresh, directly-observed qa-coord-live.ts run, not taken from any pre-existing artifact.",
    "Shared-block blast radius: TextContent's 'policy' variant (the only variant touched by this diff) is also used by /environmental-policy/. Spot-checked: clone and live both have exactly 30 <li> in the environmental-policy article content; clone's lists there are flat <ul> (lower-alpha, no li>ul nesting), so the new '[&_li_ul]:list-[lower-roman]' / '[&_li_ul>li]:!my-0' rules are no-ops for that page - confirmed no regression. /terms-and-conditions/ was not independently re-checked by this review (relied on the round-1 report's source-level reasoning that the CSS is scoped to variant='policy' only, which is correct per Object.values(variants[variant]) in Component.tsx - but this claim about terms-and-conditions' rendered output specifically was not re-verified empirically here, only the environmental-policy claim was)."
  ]
}
