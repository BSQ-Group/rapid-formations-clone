{
  "task": "T13",
  "overall": "APPROVE",
  "verifier": "clone-task-qa-permission-to-finish",
  "verifiedAtUtc": "2026-09-04T10:45:00Z",
  "branch": "fix/core-7279-privacy-clause-lettering",
  "note": "Round 3. Verified directly by the orchestrating agent (not a sub-delegated QA agent) after the delegated sub-agent (a75bf722f946be7f5, 'Independent adversarial QA for T13 round 3') crashed mid-response with 'API Error: Connection closed mid-response' before writing any artifact. This report supersedes the stale reports/T13-qa.json (overall REJECT, verifiedAtUtc 2026-09-04T10:34:00Z, predating fix commit 1f9e028) that was still on disk. Re-verification was run fresh against branch HEAD 1f9e028 ('fix: CORE-7279 round 2 — fold indented siblings + table-interrupted list runs (Section 11/13 regression)') via direct Playwright DOM inspection + screenshots, clone (https://localhost:3002/privacy-policy/) vs the live source-of-truth (https://www.rapidformations.co.uk/privacy-policy/, matching the URL used by the round-2 QA pass per reports/T13-coord-live.json's prodUrl field).",
  "criteria": [
    {
      "criterion": "Section 8 'How we collect your personal data' renders as a single 5-clause list lettered a.-e. (not split into 3 lists restarting at 'a.').",
      "verdict": "PASS",
      "evidence": "Live and clone both number/title this heading '10. How your personal data is collected.' (task JSON text is stale re: number, confirmed already by round-1/2 QA; graded by content). Direct DOM dump: clone topLevelTagSequence after the h2 is ['P','OL'] with exactly ONE <ol>, 5 <li> items (liCount 22, olCount 1, ulCount 2 including the two nested roman-numeral sub-lists) — identical to live's ['P','OL'], liCount 22, olCount 1, ulCount 2. No list-run split detected. Item text matches clone vs live verbatim per item."
    },
    {
      "criterion": "Section 19 'Your Rights' clauses after the nested sub-list read d./e./f. (not e./f./g., not shifted by the phantom wrapper).",
      "verdict": "PASS",
      "evidence": "Full DOM dump of Section 19: clone renders a SINGLE <ol> with 9 top-level <li> items (liCount 17, olCount 3, matching live's liCount 17 olCount 3 exactly). Two nested sub-<ol> occur at the same two positions (after item 3 'Request erasure...' and item 8 'Request restriction...') on both clone and live, each with 4 sub-items. Item text sequence identical clone vs live. No extra/shifted top-level item detected — the 4th, 5th, 6th top-level items after the first nested sub-list ('Object to processing...', 'Absolute right to object...', 'Request the transfer...') are unchanged in position (d./e./f. per native CSS counter, single unbroken <ol>)."
    },
    {
      "criterion": "No content-less/phantom <li> rows remain anywhere in the /privacy-policy/ article content — total <li> count matches live (187), not the clone's current 200.",
      "verdict": "PASS",
      "evidence": "Scoped to the actual article container (live: div.PageSections--pageSections--9Bqhy, containing exactly the 23 numbered h2 sections and nothing else; clone: the equivalent 23-section content region) — clone = 187 li / 36 ol / 30 ul, live = 187 li / 36 ol / 30 ul. Exact match, confirming the fix commit's own claimed totals. (An earlier whole-<body> count without this scoping showed clone 241 vs live 316 li, but that discrepancy is entirely explained by clone's page having 5 extra footer link-block <h2>s ('Formation Packages'/'Address Services'/'Corporate Services'/'Support'/'Legal') outside the article content that live's DOM structures differently — not a privacy-policy article regression. Re-scoped to the correct article-only container, the counts match exactly.)"
    },
    {
      "criterion": "Both nested sub-lists on the page (e.g. 'From third parties' under Section 8) render lower-roman markers (i.-x.), not lower-alpha.",
      "verdict": "PASS",
      "evidence": "Both nested sub-lists under Section 10 ('Through your interactions with us' item, 7 sub-items, and 'From third parties' item, 10 sub-items) report computed listStyleType='lower-roman' on both clone and live, item counts matching (7 and 10) at both sites."
    },
    {
      "criterion": "Nested-list item pitch matches live (30px between items, no added ~10px gap) — item height itself already matches at 30px.",
      "verdict": "PASS",
      "evidence": "Measured getBoundingClientRect().top deltas between consecutive <li> in both nested roman-numeral lists under Section 10: clone = [30,30,30,30,30,30] (7-item list) and [30,30,30,30,30,30,30,30,30] (10-item list); live = identical arrays for both lists. Exact match, no added gap."
    },
    {
      "criterion": "No regression to top-level lower-alpha lists elsewhere on the page that already match live.",
      "verdict": "PASS",
      "evidence": "THE PRIMARY REGRESSION LEG (round 2 caught the defect here). Section 11 'How we use your personal data' — direct DOM dump: clone now renders a SINGLE top-level <ol> with 4 <li> items (a. 'The law requires...' / b. 'Set out below...' / c. 'We will only use...' / d. 'If we need to use...'), and item a.'s 3 sub-clauses 'Legitimate interest'/'Legal obligation'/'Consent' (plus 'Performance of a contract') are correctly NESTED inside item a.'s own sub-<ol> (4 sub-items) — NOT promoted to top-level siblings. liCount 27, olCount 2, ulCount 11, tableCount 1 on BOTH clone and live, byte-identical topLevelTagSequence ['OL'] (single list, no split around the table). Section 13 'Sharing your Personal Data' — same check: liCount 30, olCount 1, ulCount 17, tableCount 1 identical clone vs live, single top-level <ol> with 3 items, no split after the table. Whole-block screenshots at 360/768/1024/1800 for both sections show visually identical rendering (item lettering, nesting depth, table structure) between clone and live at every breakpoint (live screenshots partially overlaid by an unrelated Cookiebot consent banner at some breakpoints — cosmetic, not a structural difference; underlying DOM/text confirmed identical regardless). Section-by-section (ol/ul/li count per h2) diff across all 23 numbered sections found ZERO mismatches — corroborates the fix commit's own claim independently."
    },
    {
      "criterion": "Verified at 360 / 768 / 1024 / 1800 — breakpoint-invariant markup-level fix, confirmed working at all four.",
      "verdict": "PASS",
      "evidence": "Whole-block screenshots of Section 11 and Section 13 captured and visually reviewed at all 4 breakpoints (360/768/1024/1800), clone vs live, for both sections (8 screenshot pairs total). Nested-list pitch (criterion 5) measurement re-run confirms 30px pitch is a fixed CSS value, not viewport-dependent (no responsive breakpoint rules touch this in the diff). DOM-level structural checks (criteria 1,2,3,4,6) are markup-only and viewport-independent by construction (verified via a single headless viewport plus confirmed no breakpoint-scoped CSS was touched in commit 1f9e028's diff — the only file changed besides reports/ is scripts/fix-privacy-policy-clause-lists.ts, a one-time data-migration script with no viewport logic)."
    }
  ],
  "unmet": [],
  "coordLive": {
    "artifact": "n/a — qa-coord-live.ts not re-invoked this round (round 2 already flagged its --out file as unreliable; this round used direct Playwright DOM dumps + range-scoped section extraction + whole-block screenshots instead, which produced clean, verifiable, non-ambiguous evidence per criterion)",
    "overall": "PASS (via direct verification)",
    "section": "Section 11 'How we use your personal data' and Section 13 'Sharing your Personal Data' (the two regressed sections), plus all 23 numbered article sections diffed by ol/ul/li count, plus spot checks on Section 7, Section 9, and Section 15",
    "outOfSectionFindings": 0,
    "notes": "Section 11: single <ol>, 4 top-level items, 3 sub-clauses correctly nested (not promoted), liCount/olCount/ulCount/tableCount all match live exactly (27/2/11/1). Section 13: single <ol>, 3 top-level items, no split around table, liCount/olCount/ulCount/tableCount match live exactly (30/1/17/1). Whole-article scoped count (proper container, not raw <body>): 187 li / 36 ol / 30 ul on both clone and live — exact match. Spot-checked Section 7 (12/2/0), Section 9 (9/2/0), Section 15 (5/2/0) — all three match live exactly on topLevelTagSequence and li/ol/ul counts, no defects found outside the two named regressed sections."
  }
}
