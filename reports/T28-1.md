# T28 / CORE-7358 — round 1: defect no longer reproduces, no code change made

**1. Task:** T28 — FAQ banner block missing entirely on `/faqs/limited-by-guarantee/` and
`/faqs/limited-by-shares/`. **Ticket:** [CORE-7358](https://linear.app/bsq-group/issue/CORE-7358/rf-faqslimited-by-guarantee-and-faqslimited-by-shares-faq-banner-block)
(raised by control at USER request, satisfying AC#1).

**2. Verdict: already resolved — no code change required.** Both pages currently render
the FAQ banner correctly on `rapid-formations-clone.vercel.app`, matching live in
position, crop and size. This mirrors the T9/CORE-7013 precedent in this same repo
(`reports/T9-verification.md`) — a defect that was real when filed and stopped
reproducing by the time it was picked up.

**3. AC#2 finding (Payload confirmation, done FIRST as instructed):** Queried the shared
MongoDB directly (`pages` collection) rather than reading rendered HTML:

- `limited-by-guarantee` and `limited-by-shares` page docs **both have a `titleBanner`
  block present** in `layout`, with `variant: "imageContained"` and an `image` ref to a
  media doc that exists, is `1170×250`, and has all `sizes` populated
  (`8ddb95c0-2a8e-47de-8a50-6c09f0ebbf18.png` / `12b0e707-b7f0-4756-afe1-a5c6ffca8cfe.png`).
- Media `createdAt` is `2026-08-06` (predates the 2026-08-24 finding). Page doc
  `updatedAt` is **2026-09-01T13:06** — i.e. **after** the 2026-08-24 P2 filing and
  **before** this task started. Something (a manual Payload admin edit or another
  lane's unrelated data touch) added the `titleBanner` block back to these two page
  documents in that window. There is no corresponding git commit because Payload
  content lives in Mongo, not in the repo — the fix was data-only and untracked.
- It **was** a CMS seeding gap, not a code defect (confirming the task's own
  hypothesis) — and that gap has since been closed, outside of any task/PR I can find.
- Cross-checked variant distribution across all 23 FAQ topic pages: `imageContained` is
  the majority variant (20/23, including several that render fine), so the earlier
  "no banner" symptom was never a variant-handling bug in `TitleBanner`'s component code
  — ruling out a code-level cause entirely.

**4. Challenges/blockers:**
- AC#1 (Linear ticket) was genuinely blocked on session auth for the `linear` MCP;
  resolved by control/user raising CORE-7358 externally — no further action needed from
  me there.
- Local dev server (port 3005) hit the known repo-wide `TurbopackInternalError: Symlink
  [project]/node_modules is invalid, it points out of the filesystem root` on the one
  permitted restart attempt. Per the health-gate's documented workaround, did not retry;
  verified instead against the Vercel preview/prod deployment
  (`rapid-formations-clone.vercel.app`), which is live and unaffected by the worktree
  symlink issue. `bun ts` was already clean before this (checked in round 0).
- The live-site headed capture at 1024 (`live-lbg-1024.png`) rendered a dark
  placeholder box instead of the photo — a capture-timing artifact (image still
  lazy-loading when the headed screenshot fired), not a real live-site defect; 360 and
  1800 both captured cleanly on live and matched the clone pixel-for-pixel in position/
  crop/size, so this doesn't change the verdict.

**5. Files changed:** none in `src/`. This report only.

**6. AC status:**
| AC | Result |
|---|---|
| 1. Linear ticket raised and linked | DONE (by control) — CORE-7358 |
| 2. Confirm Payload seeding gap vs code defect | DONE — confirmed seeding gap, **already closed** in Mongo as of 2026-09-01 |
| 3. Both pages render banner matching live (position/crop/size, 1170×250 desktop) | **Already passing** — verified live |
| 4. Banner served ≥ displayed size | **Already passing** — master 1170×250 = displayed 1170×250, same as the other 21 pages |
| 5. No change to other 21 pages | **N/A — no change made anywhere** |

**7. Verification evidence:** `pw-capture` screenshots at 360/768/1024/1800 for both
pages, clone (`rapid-formations-clone.vercel.app`) vs live (`www.rapidformations.co.uk`,
`--headed`), saved under the session scratchpad
(`t28-repro/{clone,live}-{lbg,lbs}-{360,768,1024,1800}.png`). At 360 and 1800 (the two
breakpoints captured cleanly on both sides) the banner position, crop and size are
visually identical between clone and live on both pages. Raw production HTML for
`/faqs/limited-by-guarantee/` also confirms a server-rendered `<img>` for the correct
master (`width="1170" height="250"`, `class="block h-auto w-full object-cover
max-h-[260px]"`) — not client-side-only or conditionally hidden.

**8. Recommendation:** Close **CORE-7358** as already-resolved (data-only fix, no PR),
with this report as the evidence trail — same disposition as CORE-7013/T9. Set **T28**
back to a non-running/void-equivalent state; I have not touched `tasks/T28.json` beyond
what control already wrote, since I'm not confident of the exact CLI verb for this
disposition and didn't want to guess against shared task-state.

**9. Ship path:** **No PR opened.** There is no code diff to ship — opening a PR with an
empty/no-op diff against a bug ticket that no longer reproduces would misrepresent the
fix. This is the "engine-first is off, but that doesn't mean force a change" case:
the defect is gone, not hidden by the wrong diagnosis.

**10. Task-quality feedback:** The task's own text got this right — it explicitly said
"Most likely the banner block was never added to these two pages in the CMS (a seeding
gap)... **Confirm in Payload before treating it as code.**" That instruction was sound
and I followed it. What was missing: nothing in the task anticipated that the CMS gap
might have **already been closed by someone else** between filing (2026-08-24) and
pickup (2026-09-04) — an 11-day gap during which the shared Mongo content (which isn't
git-tracked, so has no diff/blame trail) changed. Suggestion: when a bug task's evidence
is more than a few days old and describes CMS/data state (as opposed to code), the
worker's first move should be a fresh live-vs-clone screenshot diff *before* diagnosing
root cause — I did the Payload query first per the task's instruction, which was correct,
but a 30-second screenshot check would have surfaced "already fixed" even faster than
the Mongo query did. No rule change needed beyond noting this in case it recurs — this is
the second time in this repo's history (after T9) that a filed defect stopped
reproducing before pickup, so it may be worth planning verifying currency of `bug`-tagged
tasks with a quick live check as part of task creation/triage, not just at
implementation.

**11. Step timing & bottleneck** (measured from `.claude/HEARTBEAT.md`, `grep 'T28'`):

| Step | Duration | Span (HH:MM:SS) |
|---|---|---|
| ac (blocked → unblocked, spans the control round-trip) | 24m33s | 11:17:04 → 11:41:37 |
| repro (Payload/Mongo investigation) | 1m15s | 11:41:41 → 11:42:56 |
| build (dev server restart attempt, hit known Turbopack issue) | 1m29s | 11:42:56 → 11:44:11 |
| capture (prod current-state screenshot) | 1m05s | 11:44:40 → 11:45:45 |
| verify (clone vs live, 4 breakpoints × 2 pages) | 1m41s | 11:45:45 → 11:47:26 |
| **Total (repro→verify, excluding the ac blocker wait)** | **~5m20s** | 11:41:41 → 11:47:26 |

**Bottleneck:** none of the working steps — the round's wall-clock was dominated by the
AC#1 Linear-MCP blocker/round-trip (24m33s), which is external (session auth), not a
process inefficiency in the repro→verify loop itself.
