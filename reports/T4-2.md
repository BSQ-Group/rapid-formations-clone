# T4 (CORE-7119) — gate-and-PR lane report, round 2

## 1. Rebase + push
`git rebase origin/main` clean. `git diff origin/main..HEAD --stat` = only the two fix files
(`src/app/(frontend)/globals.css`, `src/blocks/TextContent/TextContent.styles.ts`). Force-pushed with
`--force-with-lease`. HEAD is `90e8cc3`.

## 2. Dev server health-gate
:3001 was down at start of this round (no process listening). One restart (`PORT=3001 bun run dev`
backgrounded) → healthy, HTTP 200 within the generous wait window. No retry loop needed.

## 3. coord-live gate — VERDICT: does NOT pass, and I do not believe it can, for a reason unrelated
to this fix. Full evidence below (control asked for evidence, not a guess).

Command run exactly as specified (`--section "We may also request verification documents from other
individuals"`, viewports 360/768/1024/1800). Result written to
`<task-state-root>/reports/T4-coord-live.json` (TASK_STATE_ROOT redirected it there, not into this
worktree — confirmed that's the tool's own resolution, not an error).

**gateSection was NOT null at any breakpoint** — this is not the T7 unscoped-whole-page trap:

| vp | gateSection | overall |
|---|---|---|
| 360 | 301 | FAIL |
| 768 | 243 | FAIL |
| 1024 | 223 | FAIL |
| 1800 | 284 | PASS |

The gate correctly scoped to the containing block (a "block" = one coarse top-level wide container —
here, the whole `TextContent` rich-text block that holds the "Note:" paragraph, several other
paragraphs, headings, and lists — that's the granularity the tool's `blockOf()` uses, not something I
can subdivide via a different anchor string). Filtering findings to `block === gateSection` at 360vp
gives exactly 4 gating findings, none of which are the "Note:" text itself:

```
[size] <li> "after your order has been processed..." Δh=10px
[size] <li> "forward the relevant link..." Δh=10px
[size] <li> "each individual will click the link..." Δh=10px
[size] <h3> "When might we ask for additional information?" Δh=10px
```

No finding anywhere in the report matches the "Note:" key — the changed element has **zero** drift.
The drift is on unrelated list items and a heading elsewhere in the same coarse block.

**Is this pre-existing, or did my fix cause it?** My fix only changes `color` (a `globals.css` rule
adding `@apply text-[color:...]` to `strong`/`b`, plus removing a wrong color override in
`TextContent.styles.ts`) — no width/height/padding/line-height/font-weight touched anywhere. So it
cannot mechanically produce a height delta. Confirmed directly: I measured the flagged
`h3` ("When might we ask for additional information?") on a **clean, unfixed `origin/main` baseline**
running at `https://localhost:3010` (a separate worktree control stood up for this exact purpose,
pinned to `origin/main` @ `d52af37`, no lane fixes). At 360vp its rendered height is **62px** — the
same 62px the coord-live report captured for the clone-with-my-fix, both vs. prod's 52px. The delta is
identical with or without my fix. **Agreeing with T1's finding: this FAIL is pre-existing
clone-vs-live baseline drift, not something either of our fixes caused.**

Per the brief's own instruction ("If section scoping genuinely cannot be made to resolve: STOP and
report that honestly... a fabricated PASS is not [an option]") and control's explicit stop order, I did
**not** pass `--coord-gate-waived` (that waiver is documented for a non-visual bug; this is a visual
bug that scoped correctly but fails on unrelated content) and I have **not** attempted to force a PASS.

**Implication for the gate as a ship-gate on this repo:** for a task that is a single CSS rule inside a
large hand-authored rich-text block, this gate cannot currently isolate "did MY changed element drift"
from "does anything else in the same block already differ from prod" — the block granularity is too
coarse. That's a genuine tooling gap, not a defect in this fix. Worth a Planning/Optimizer note; not
mine to fix from this lane.

## 4. Colour verification (independent of coord-live, which measures position/size, not colour)

Ran directly via Playwright (`--ignore-certificate-errors` + `ignoreHTTPSErrors: true` against
`https://localhost:3001`, trailing-slash redirects followed):

| Page | Bold run | Colour | Expected |
|---|---|---|---|
| `/id-requirements/` (360/768/1024/1800) | "Note:" | `rgb(49,49,49)` = `#313131` | `#313131` ✅ |
| `/id-requirements/` (all 4 bp) | paragraph around it | `rgb(89,89,89)` = `#595959` | `#595959` ✅ |
| `/director-appointment-resignation/` (360/1024) | "Please note:" | `rgb(49,49,49)` | `#313131` ✅ |
| `/whistleblowing-grievance-mechanism-policy/` (360/1024) | all 7 list lead-ins | `rgb(49,49,49)` | `#313131` ✅ |
| `/vat-registration/` (360/1024) | "Buy Now" | `rgb(49,49,49)` | `#313131` ✅ |
| `/business-telephone/` (360/1024) | "Improve your corporate image" | `rgb(49,49,49)` | `#313131` ✅ |
| `/business-telephone/` | "Enhance your corporate image..." (already-correct run) | `rgb(49,49,49)` | unaffected ✅ |
| `/terms-and-conditions/` (360/1024) | 12-sample of 335 clause refs | `rgb(49,49,49)` | `#313131` (was `#363636`) ✅ |
| `/cookies-policy/` (1024) | 8 heading-nested bolds (h2/h3) | `rgb(54,54,54)` == heading's own colour (inherit) | NOT forced to `#313131` ✅ |
| `/ico-registration/` (1024) | 4 feature-list bolds | `rgb(49,49,49)` | already-correct, unaffected ✅ |
| review-figure "4.8"/"4.9" on `/`, `/id-requirements/`, `/business-telephone/` | | `/`,`/id-requirements/`: not present as bold; `/business-telephone/`: `rgb(89,89,89)` | `#595959`, correct ✅ |
| id-requirements bold run — weight/size/line-height at all 4 bp | | `700`/`20px`/`30px`, paragraph `400`/`20px`/`30px` | unchanged, matches ticket's "already matched" note ✅ |
| `wholeBlockRegression.strongCountInParagraph` at all 4 bp | | `1` | no duplicate-bold artifact ✅ |

All 5 acceptance criteria from `tasks/T4.json` are satisfied by this direct evidence.

**Bonus finding (the "cheap one measurement" ask from round 1):** director-appointment-resignation's
FAQ answerText bold ("Please note:") computed colour is `rgb(49,49,49)` = `#313131`, confirming the
FAQ answer container's `.payload-richtext` class IS covered by the shared rule on this branch.
**Proposal P1 (claiming a separate fix is needed in `FAQs.styles.ts`) is obsolete — void it.**

## 5. Formal QA-permission-to-finish gate — APPROVE (obtained after this report was drafted; recording verbatim)

Three fresh-subagent attempts total, the first two unsuccessful for reasons outside this lane's
control, the third (apparently a nested continuation of attempt 2, notified separately) succeeded:
- Attempt 1: returned immediately with "You've hit your monthly spend limit" — no artifact, 2 tool
  calls, 32s.
- Attempt 2: ran 102s / 19 tool calls / 67k tokens, built the verification script section 4 above is
  drawn from, then stalled on a confused wait-for-background-notification loop and returned with no
  artifact.
- Attempt 3 (notified after this report's draft, same task family as attempt 2): ran 249s / 32 tool
  calls / 80.6k tokens, wrote `reports/T4-qa.json` — **overall: APPROVE**, all 5 criteria PASS, evidence
  matching section 4 above almost line for line (independently re-measured, not copied). Also wrote
  `reports/T4-visual-qa.json` and `reports/T4-qa-attempts.jsonl`.

**Note on this QA gate's internal "coordLive" field:** it self-reports `overall: PASS` for its own
"whole-block regression leg," but this is a *different, self-run* check (computed-style equality +
screenshots on the clone only) — it explicitly discloses it never reached rapidformations.co.uk. It is
**not** the same artifact as `reports/T4-coord-live.json` (the actual `qa-coord-live.ts` prod-vs-clone
tool the rails read), which still reports `overall: FAIL` per section 3. Do not conflate the two — the
rails-checked coord-live gate is unchanged by this APPROVE.

## 6. PR #87 — updated, confirmed by control as the reference format. Not touched further this round.

## 7. Post-push checks
- `gh pr checks 87`: Run Unit Tests pass, Vercel pass, Vercel Preview Comments pass. Build green.
- Cursor Bugbot: no `Cursor Bugbot` entry ever appeared in `gh pr checks` (checked twice, ~90s apart)
  and no bot comments exist on the PR. Treated as not-triggered for this PR; did not poll further
  (no-CI-polling-loop convention).
- Linear "In Review": Linear MCP not authenticated in this environment — skipped, as flagged in the
  original brief.

## 8. Status — NOT changed. T4 remains at whatever status control set it to.
Per control's explicit order: did not run `set-task-status.ts T4 awaiting-user`. The rails would
reject it anyway (`coord-live overall FAIL`), and control has said it owns that decision.

## 9. Field 10 — was the gate-and-PR split easier than all-in-one?
Yes for the fix itself (nothing to redo — the color rule was correct on first read). But this round
burned a large fraction of context on: (a) discovering the coord-live tool's block-granularity ceiling
by hand (no shortcut existed — had to read `qa-coord-page.ts` source to understand `blockOf()`/
`gateSection` semantics before I could tell "scoped-but-failing-on-unrelated-content" apart from
"unscoped"), and (b) two failed QA-subagent spawns that produced zero usable signal for real cost
(monthly-spend-limit rejection, then a 67k-token stall). Neither is a fix-lane problem; both are
gate/tooling reliability gaps worth Planning/Optimizer attention:
- The coord-live "block" concept has no way to scope tighter than "the whole rich-text block" — a
  single-CSS-property fix inside a large TextContent block can never get an isolated PASS if any
  unrelated pre-existing drift exists anywhere else in that block, even with zero drift on the changed
  element itself.
- The permission-to-finish QA subagent step failed twice in a row for non-content reasons (account
  limit, then agent self-confusion) — if that's representative, it's not a reliable gate as currently
  invoked via a spawned Agent call.

## 10. Verbatim gate outputs
`coord-live` CLI summary (full JSON at `<task-state-root>/reports/T4-coord-live.json`):
```
coord-live FAIL  https://localhost:3001/id-requirements/
  @360: FAIL (gate=section:301)  coverage[checked=38 missing=27 extra=36 skipped=0] ...
  @768: FAIL (gate=section:243)  coverage[checked=38 missing=27 extra=36 skipped=0] ...
  @1024: FAIL (gate=section:223) coverage[checked=79 missing=27 extra=31 skipped=0] ...
  @1800: PASS (gate=section:284) coverage[checked=86 missing=27 extra=35 skipped=0] ...
```
(missing/extra counts are almost entirely `<img>` — advisory-only per the tool's own gating rule,
`kind !== 'img'` for the `missing` class; they did not factor into the FAIL — the 4 `size` findings at
360vp, listed in §3, are what gated it.)
